// import Counter from './counter';
import counter from './counter';
import './readButton';
import './blueButton';

// console.log(Counter.getCount());
/**
 * When we invoke the increment method in either readButton.ts or blueButton.ts, the value of counter is the same.
 */

console.log(counter.getCount());
